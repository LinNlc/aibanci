# SSE 反向代理配置片段（需放置在 HTTPS server{} 内）
location /api/sse {
    proxy_pass http://${PHP_UPSTREAM};
    proxy_http_version 1.1;
    proxy_set_header Connection "";
    proxy_buffering off;
    proxy_cache off;
    gzip off;
    add_header X-Accel-Buffering no;
    proxy_read_timeout 3600s;
}
